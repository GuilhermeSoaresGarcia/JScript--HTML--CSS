/* Tipos de dados primitivos em JS:
String, number, undefined, null, boolean, symbol */

const nome = 'Guilherme'; // String
const numero = 10; // Number
let variavelQualquer; // undefined -> não aponta para local nenhum na memória
const variavelQualquer = null; // undefined -> não aponta para local nenhum na memória
const valorBooleano = false; // Boolean = true, false (lógico)

// Symbol será revisado mais para frente